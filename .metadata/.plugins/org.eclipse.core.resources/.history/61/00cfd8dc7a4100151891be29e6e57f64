/*
 * CutRod.cpp
 *
 *  Created on: Aug 13, 2015
 *      Author: user
 */
#include<iostream>
#include<limits.h>
using namespace std;
int cutRod(int price[],int n){
	if(n<=0){
		return 0;
	}
	int max_val=INT_MIN;
	for(int i=1;i<=n;i++){
		max_val= max(max_val,price[i-1]+cutRod(price,n-i));
	}
	return max_val;
}

void cutRod_DP(int price[],int n){
	int cutRod[n+1];
	cutRod[0]=0;
	for(int i=1;i<=n;i++){
		max_val=max(price[i-1]+cutRod[n-i]);
	}
}
int main(){
	int arr[] = {1, 5, 8, 9, 10, 17, 17, 20};
	int size = sizeof(arr)/sizeof(arr[0]);
	cout<<"Maximum profit is"<<cutRod(arr,size);
	return 0;
}
